# This file maps my laptops menu key to the right meta key.
#
# For rebinding keys for weird keyboards checkout this guide:
# https://github.com/hakimel/reveal.js/wiki/Linux-udev-keys-binding
#
# To install place this file in '/etc/udev/hwdb.d/' like so:
# sudo cp 61-*.hwdb /etc/udev/hwdb.d/
#
# Then reload udev:
# sudo udevadm hwdb --update
# sudo udevadm trigger

evdev:input:b0011v0001p0001*
 KEYBOARD_KEY_dd=rightmeta
