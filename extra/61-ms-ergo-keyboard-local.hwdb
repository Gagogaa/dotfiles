# This maps the nub on the Microsoft Ergonomic Keyboard 4000 to PGup PGdn 
#
# For other weird keyboards checkout this guide:
# https://github.com/hakimel/reveal.js/wiki/Linux-udev-keys-binding
#
# To install place this file in '/etc/udev/hwdb.d/' like so:
# sudo cp 61-*.hwdb /etc/udev/hwdb.d/
#
# Then reload udev:
# sudo udevadm hwdb --update
# sudo udevadm trigger

evdev:input:b0003v045Ep00DB*
 KEYBOARD_KEY_c022d=pageup
 KEYBOARD_KEY_c022e=pagedown
